spring:
  application:
    name: taskflow

  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:taskflow}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: ${DB_USER:root}
    password: ${DB_PASS:secret}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      pool-name: TaskFlowPool
      maximum-pool-size: 20
      minimum-idle: 5
      idle-timeout: 30000
      connection-timeout: 20000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
        default_batch_fetch_size: 25
        order_inserts: true
        order_updates: true
        jdbc:
          batch_size: 25

  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql

taskflow:
  jwt:
    secret: ${JWT_SECRET:ThisIsAVeryLongSecretKeyForTaskFlowThatIsAtLeast256BitsLong!}
    expiration-ms: 86400000   # 24h

  executor:
    core-pool-size: 4
    max-pool-size: 16
    queue-capacity: 500
    keep-alive-seconds: 60
    thread-name-prefix: taskflow-worker-

  assignment:
    strategy: LEAST_LOADED    # LEAST_LOADED | ROUND_ROBIN | PRIORITY_BASED

server:
  port: 8080
  servlet:
    context-path: /api

logging:
  level:
    root: INFO
    com.taskflow: DEBUG
    org.hibernate.SQL: WARN
  pattern:
    console: "%clr(%d{HH:mm:ss.SSS}){faint} %clr(%5p) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n"
